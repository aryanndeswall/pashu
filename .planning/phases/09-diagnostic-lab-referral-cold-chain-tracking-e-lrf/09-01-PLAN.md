---
phase: 09-diagnostic-lab-referral-cold-chain-tracking-e-lrf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/lab.py
  - backend/app/models/__init__.py
  - backend/app/schemas/lab.py
  - backend/app/schemas/__init__.py
  - backend/app/services/lab_service.py
  - backend/app/services/__init__.py
  - backend/app/api/v1/labs.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_labs.py
autonomous: true
requirements:
  - LAB-01
  - LAB-02
  - LAB-03
user_setup: []

must_haves:
  truths:
    - "e-LRF requisitions are generated with unique ID format LRF-YYYYMMDD-XXXX and QR payload containing sample metadata"
    - "Cold-chain SLA engine computes 48-hour countdown, elapsed hours, and temperature health status (OPTIMAL, WARNING, BREACHED)"
    - "Submitting a POSITIVE RT-PCR/ELISA test result escalates case status to LAB_CONFIRMED"
  artifacts:
    - "backend/app/models/lab.py"
    - "backend/app/schemas/lab.py"
    - "backend/app/services/lab_service.py"
    - "backend/app/api/v1/labs.py"
    - "backend/tests/test_labs.py"
  key_links:
    - "lab_service evaluates cold chain SLA time-delta from collection timestamp"
    - "labs API router mounted under /api/v1/labs"
---

<objective>
Implement the backend e-LRF Requisition, Cold-Chain SLA Tracking, and Diagnostic Result Escalation engine: define LabRequisition ORM models and Pydantic schemas, implement 48-hour cold-chain countdown calculation and temperature compliance logic, build REST endpoints for requisition creation, temperature checkpoint logging, and RT-PCR/ELISA result submission, verified with automated pytests.

Purpose: Establishes the authoritative diagnostic custody record and automates laboratory confirmation for suspected animal disease outbreaks.
Output: Lab database models, schemas, cold-chain SLA service, API endpoints, and pytest suite.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-diagnostic-lab-referral-cold-chain-tracking-e-lrf/09-CONTEXT.md
@.planning/phases/09-diagnostic-lab-referral-cold-chain-tracking-e-lrf/09-RESEARCH.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-01 | Sample Degradation | lab_service.py | Samples analyzed past 48h SLA result in false negative outcomes | Compute SLA health explicitly; flag `BREACHED` and warn pathologist on result entry. |
| T-09-02 | Unverified Case Escalation | labs.py | Arbitrary user escalates case to `LAB_CONFIRMED` | Validate test assay type (RT-PCR, ELISA, etc.) and pathologist identification. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build Lab ORM Models, Schemas & Cold-Chain SLA Engine</name>
  <files>backend/app/models/lab.py, backend/app/models/__init__.py, backend/app/schemas/lab.py, backend/app/schemas/__init__.py, backend/app/services/lab_service.py, backend/app/services/__init__.py</files>
  <read_first>.planning/phases/09-diagnostic-lab-referral-cold-chain-tracking-e-lrf/09-RESEARCH.md, backend/app/schemas/cluster.py</read_first>
  <action>
    1. Create `backend/app/models/lab.py`:
       - `LabRequisition`:
         - `id`: Integer PK
         - `requisition_id`: String(50) unique index (e.g. `LRF-YYYYMMDD-XXXX`)
         - `animal_tag_id`: String(12) index
         - `incident_id`: Optional String(50) index
         - `cluster_id`: Optional String(50) index
         - `vet_id`: String(50)
         - `village_name`: String(100)
         - `district_name`: String(100)
         - `sample_type`: String(50)
         - `suspected_disease`: String(50)
         - `preservative`: String(100)
         - `destination_lab`: String(100)
         - `status`: String(30) (PENDING, IN_TRANSIT, RECEIVED, TESTING, LAB_CONFIRMED, NEGATIVE)
         - `transit_temp_c`: Float default 4.0
         - `temp_breached`: Boolean default False
         - `collected_at`: DateTime(timezone=True)
         - `dispatched_at`: DateTime(timezone=True)
         - `received_at`: Optional DateTime(timezone=True)
         - `test_type`: Optional String(50)
         - `test_result`: Optional String(30)
         - `result_notes`: Optional Text
         - `confirmed_at`: Optional DateTime(timezone=True)
         - `qr_payload`: Text
         - `created_at`: DateTime
         - `updated_at`: DateTime
    2. Export `LabRequisition` in `backend/app/models/__init__.py`.
    3. Create `backend/app/schemas/lab.py`:
       - `ColdChainStatus`: Literal["OPTIMAL", "WARNING", "BREACHED"]
       - `RequisitionStatus`: Literal["PENDING", "IN_TRANSIT", "RECEIVED", "TESTING", "LAB_CONFIRMED", "NEGATIVE"]
       - `LabRequisitionCreate`: animal_tag_id, sample_type, suspected_disease, preservative, destination_lab, vet_id, village_name, district_name, incident_id, cluster_id, collected_at, initial_temp_c
       - `TemperatureLogCreate`: temperature_c, location_checkpoint, logged_by
       - `LabResultSubmit`: test_type, test_result (POSITIVE | NEGATIVE | INCONCLUSIVE), pathologist_id, notes
       - `ColdChainMetrics`: elapsed_hours, remaining_hours, percent_elapsed, cold_chain_status, is_breached, advisory_message
       - `LabRequisitionResponse`: all fields from model + computed `cold_chain: ColdChainMetrics`
    4. Export lab schemas in `backend/app/schemas/__init__.py`.
    5. Create `backend/app/services/lab_service.py`:
       - `compute_cold_chain_sla(collected_at: datetime, current_temp_c: float, now: Optional[datetime] = None) -> ColdChainMetrics`:
         - SLA limit = 48.0 hours.
         - Computes elapsed hours and remaining hours.
         - Checks temperature: 2°C–8°C optimal; 8°C–12°C or >36h warning; >12°C or >=48h breached.
       - `generate_requisition_id() -> str`: Format `LRF-{YYYYMMDD}-{random_hex}`.
       - `generate_qr_payload(data: dict) -> str`: Compact JSON payload.
       - `create_requisition(...)`, `log_temperature(...)`, `submit_result(...)`.
       - Escalates case status to `LAB_CONFIRMED` when result is `POSITIVE`.
    6. Export `lab_service` in `backend/app/services/__init__.py`.
  </action>
  <verify>
    Run `python -c "from app.models import LabRequisition; from app.schemas.lab import LabRequisitionCreate; from app.services.lab_service import lab_service; print('Lab services initialized')"`
  </verify>
  <acceptance_criteria>
    - Lab ORM model, Pydantic schemas, and cold-chain SLA computation service exist and import cleanly
  </acceptance_criteria>
  <done>
    Lab models, schemas, and cold-chain calculation service completed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Labs API Endpoints & Automated Pytest Suite</name>
  <files>backend/app/api/v1/labs.py, backend/app/api/v1/__init__.py, backend/tests/test_labs.py</files>
  <read_first>backend/app/services/lab_service.py, backend/app/schemas/lab.py, backend/app/api/v1/__init__.py</read_first>
  <action>
    1. Create `backend/app/api/v1/labs.py`:
       - `router = APIRouter()`
       - In-memory active requisition registry for zero-dependency test and offline demo mode.
       - `POST /api/v1/labs/requisitions`: Create new e-LRF requisition, generate QR payload, compute initial SLA, return `LabRequisitionResponse`.
       - `GET /api/v1/labs/requisitions`: List requisitions with optional filters (`status`, `animal_tag_id`, `district_name`).
       - `GET /api/v1/labs/requisitions/{requisition_id}`: Retrieve detailed requisition with real-time computed `cold_chain` SLA metrics.
       - `POST /api/v1/labs/requisitions/{requisition_id}/temperature`: Log temperature checkpoint and recompute cold-chain health.
       - `POST /api/v1/labs/requisitions/{requisition_id}/result`: Submit lab test result (RT-PCR/ELISA). If POSITIVE, escalate status to `LAB_CONFIRMED` and record confirmation timestamp.
    2. Mount `labs_router` in `backend/app/api/v1/__init__.py` under prefix `"/labs"`.
    3. Create `backend/tests/test_labs.py`:
       - Test e-LRF creation and QR payload generation.
       - Test cold-chain SLA calculations (10h -> 38h left / OPTIMAL, 40h -> 8h left / WARNING, 50h -> 0h left / BREACHED).
       - Test temperature threshold breach (>12°C flags BREACHED).
       - Test RT-PCR POSITIVE escalates status to `LAB_CONFIRMED`.
       - Test RT-PCR NEGATIVE sets status to `NEGATIVE`.
       - Test 404 on nonexistent requisition ID.
  </action>
  <verify>
    Run `python -m pytest backend/tests/test_labs.py -v` to ensure 100% tests pass.
  </verify>
  <acceptance_criteria>
    - Labs API mounted under `/api/v1/labs`
    - All tests in `backend/tests/test_labs.py` pass cleanly
  </acceptance_criteria>
  <done>
    Labs endpoints and automated pytests completed and verified.
  </done>
</task>

</tasks>
