---
phase: 03-hardware-sensor-bridges-camera-gps-voice
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - mobile/src/components/media/CameraCaptureCard.tsx
  - mobile/src/components/media/VoiceRecorderCard.tsx
  - mobile/src/components/location/LocationPickerCard.tsx
  - mobile/src/views/ReportWizardView.tsx
  - mobile/src/views/ReportView.tsx
  - mobile/src/tests/ReportWizardView.test.tsx
autonomous: true
requirements:
  - SYN-02
  - SYN-03
  - SYN-04
user_setup: []

must_haves:
  truths:
    - "3-step reporting wizard guides the user through Step 1 (Syndrome selection), Step 2 (Photo and Voice evidence), and Step 3 (GPS LGD Location and Pashu Aadhaar tag)"
    - "Camera card displays live compressed photo thumbnail and green WebP size badge (<300 KB)"
    - "Voice recorder card provides an animated 8-bar audio visualizer, 30s countdown ring, and one-tap listen / re-record controls"
    - "Location card displays live GPS lock, auto-snapped LGD village, and manual village dropdown override"
    - "Final report submission saves complete syndromic draft payload to SQLite offline queue with haptic confirmation"
  artifacts:
    - "mobile/src/components/media/CameraCaptureCard.tsx"
    - "mobile/src/components/media/VoiceRecorderCard.tsx"
    - "mobile/src/components/location/LocationPickerCard.tsx"
    - "mobile/src/views/ReportWizardView.tsx"
    - "mobile/src/views/ReportView.tsx"
    - "mobile/src/tests/ReportWizardView.test.tsx"
  key_links:
    - "ReportWizardView.tsx coordinates Step 1 (SyndromeGrid), Step 2 (CameraCaptureCard + VoiceRecorderCard), and Step 3 (LocationPickerCard)"
    - "CameraCaptureCard.tsx invokes cameraService.captureLesionPhoto and displays WebP compression badge"
    - "VoiceRecorderCard.tsx invokes voiceService.startRecording / stopRecording and provides audio playback"
    - "LocationPickerCard.tsx invokes locationService.getCurrentLocation and snapToNearestLgdVillage"
    - "ReportWizardView.tsx persists finalized report to dbService (offline_sync_queue) and triggers hapticsService.hapticMedium"
---

<objective>
Build the specialized multi-sensor UI components (`CameraCaptureCard`, `VoiceRecorderCard`, `LocationPickerCard`), assemble the cohesive 3-step `ReportWizardView.tsx` replacing the static drawer, wire SQLite offline report persistence with tactile haptics, and establish integration tests verifying end-to-end multi-sensor report assembly.

Purpose: Provides low-literacy field workers and Pashu Sakhis with an intuitive, linear reporting wizard capturing rich multimodal evidence (compressed WebP photo, vernacular voice memo, snapped GPS village).
Output: Fully functional 3-step reporting wizard integrated into `ReportView.tsx` with automated test coverage.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-CONTEXT.md
@.planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-UI-SPEC.md
@.planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-RESEARCH.md
@.planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-05 | Input Validation | ReportWizardView | Incomplete or empty syndromic reports submitted without mandatory syndrome or village identification | Enforce step validation preventing progression to Step 2 without a chosen syndrome, and requiring a confirmed LGD village in Step 3. |
| T-03-06 | Denial of Service / Crash | Media Components | Memory leak from unreleased MediaStreams or audio objects in React component lifecycles | Clean up Audio objects and revoke Object URLs in `useEffect` cleanup return functions. |
| T-03-07 | Data Loss | SQLite Persistence | Local database write failure when saving offline report causing lost field telemetry | Wrap database insertion in try/catch with fallback to in-memory store and clear visual feedback if storage is full. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build Specialized Media & Location Cards (Camera, Voice, GPS LGD)</name>
  <files>mobile/src/components/media/CameraCaptureCard.tsx, mobile/src/components/media/VoiceRecorderCard.tsx, mobile/src/components/location/LocationPickerCard.tsx</files>
  <read_first>mobile/src/services/cameraService.ts, mobile/src/services/voiceService.ts, mobile/src/services/locationService.ts, .planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/components/media/CameraCaptureCard.tsx`:
       - Features single-tap capture button ("📸 जनावराचा फोटो काढा (Take Photo)") and gallery upload option.
       - Displays image thumbnail when captured, with clear badge: `WebP • {sizeKB} KB ({width}x{height})`.
       - Provides "🔄 फोटो बदला (Retake / Change)" button.
       - Enforces 52px touch targets (`.field-touch-target`).
    2. Create `mobile/src/components/media/VoiceRecorderCard.tsx`:
       - In idle state: shows prominent microphone button ("🎙️ स्थानिक व्हॉइस नोट (Record Voice Note)").
       - In recording state:
         - Displays animated 8-bar audio equalizer bars oscillating in Crimson (`#dc2626`).
         - Real-time countdown clock (`वेळ: 00:18 / 00:30`).
         - Stop button ("थांबवा (Stop Recording)").
       - In recorded state:
         - Audio review bar with Play/Pause toggle (`▶️ ऐका / ⏸️ थांबवा`).
         - Re-record button ("🔄 पुन्हा रेकॉर्ड करा").
    3. Create `mobile/src/components/location/LocationPickerCard.tsx`:
       - Top section: GPS status pill with pulsing satellite icon (`GPS अचूकता: ±8m` or warning if >100m).
       - Middle section: Auto-detected LGD village badge (`गाव: राहुरी खुर्द, ता. राहुरी, जि. अहमदनगर • LGD: 556601`).
       - Bottom section: Expandable / searchable manual village dropdown allowing override from all 26 seeded Maharashtra villages if GPS accuracy is low or indoors.
  </action>
  <verify>
    Run `npm --prefix mobile run build` to verify React components and TypeScript types compile cleanly.
  </verify>
  <acceptance_criteria>
    - `mobile/src/components/media/CameraCaptureCard.tsx` renders photo capture UI and WebP compression badge
    - `mobile/src/components/media/VoiceRecorderCard.tsx` renders waveform bars, countdown timer, and playback controls
    - `mobile/src/components/location/LocationPickerCard.tsx` renders GPS satellite status, snapped LGD village, and manual override
  </acceptance_criteria>
  <done>
    Specialized hardware sensor UI cards built with responsive Devanagari labels and 52px ergonomics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 3-Step ReportWizardView, Wire SQLite Persistence & Integration Tests</name>
  <files>mobile/src/views/ReportWizardView.tsx, mobile/src/views/ReportView.tsx, mobile/src/tests/ReportWizardView.test.tsx</files>
  <read_first>mobile/src/components/media/CameraCaptureCard.tsx, mobile/src/components/media/VoiceRecorderCard.tsx, mobile/src/components/location/LocationPickerCard.tsx, mobile/src/database/sqliteConnection.ts, .planning/phases/03-hardware-sensor-bridges-camera-gps-voice/03-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/views/ReportWizardView.tsx`:
       - Manages wizard step state: `currentStep: 1 | 2 | 3`.
       - **Step 1: लक्षण निवड (Syndrome Selection)**:
         - Visual 2-column syndrome selector grid (using `SyndromeGrid.tsx`).
         - Validation: Requires selecting a syndrome before clicking "पुढे जा (Next) ->".
       - **Step 2: पुरावे जोडणी (Media & Voice Evidence)**:
         - Integrates `CameraCaptureCard` and `VoiceRecorderCard`.
         - Shows summary of selected syndrome with anatomical badge.
         - Allows advancing to Step 3 even if media is optional, but highlights attached evidence.
       - **Step 3: स्थान व पशू आधार (Location & Tag Identification)**:
         - Integrates `LocationPickerCard`.
         - 12-digit Pashu Aadhaar RFID ear-tag numeric input with auto-formatting (`XXXX-XXXX-XXXX`).
         - Final Submit button: **"अहवाल जतन करा (Save Offline Report)"** (`.field-touch-target`, emerald accent).
       - **Submission Handler**:
         - Assembles payload: `{ report_id, syndrome_code, photo_webp_base64, photo_size_kb, audio_base64, audio_duration_sec, latitude, longitude, gps_accuracy, lgd_village_code, lgd_village_name, pashu_aadhaar_tag, created_at }`.
         - Inserts payload into `offline_sync_queue` table in SQLite (`dbService.execute`).
         - Triggers medium haptic confirmation (`hapticsService.hapticMedium()`).
         - Notifies user with success alert/toast and resets wizard to Step 1 or navigates to Dashboard.
    2. Update `mobile/src/views/ReportView.tsx`:
       - Embeds `ReportWizardView` as the primary reporting workflow in the Report tab.
    3. Create `mobile/src/tests/ReportWizardView.test.tsx`:
       - Tests complete wizard flow from Step 1 (selecting VSS syndrome) -> Step 2 (attaching mocked photo and voice) -> Step 3 (confirming LGD location and entering Pashu Aadhaar tag).
       - Verifies that clicking "अहवाल जतन करा" inserts the record into SQLite `offline_sync_queue`.
       - Tests step navigation (Back and Next buttons).
  </action>
  <verify>
    Run `npm --prefix mobile test -- --run` and ensure 100% of unit and integration tests pass.
  </verify>
  <acceptance_criteria>
    - `mobile/src/views/ReportWizardView.tsx` guides user through 3 steps with Devanagari copy and step progress bar
    - `mobile/src/views/ReportView.tsx` displays the multi-sensor wizard
    - `mobile/src/tests/ReportWizardView.test.tsx` passes with 100% green assertions
    - Full suite `npm --prefix mobile test -- --run` passes
  </acceptance_criteria>
  <done>
    3-step multi-sensor reporting wizard fully implemented, wired to SQLite offline queue, and verified by automated integration tests.
  </done>
</task>

</tasks>
