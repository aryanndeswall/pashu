---
phase: 06-livestock-registry-pashu-aadhaar-cloud-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - mobile/src/services/animalService.ts
  - mobile/src/components/animals/AnimalCard.tsx
  - mobile/src/components/animals/VaccinationTimeline.tsx
  - mobile/src/components/animals/NewAnimalModal.tsx
  - mobile/src/views/AnimalRegistryView.tsx
  - mobile/src/tests/animalService.test.ts
  - mobile/src/tests/AnimalRegistryView.test.tsx
autonomous: true
requirements:
  - REC-01
  - REC-02
user_setup: []

must_haves:
  truths:
    - "animalService queries and registers cattle offline in native SQLite local_animals table"
    - "AnimalRegistryView displays the 3-segment digital cattle passbook: My Cattle, Tag Lookup, and New Registration"
    - "Vaccination timelines compute DAHD booster schedules and render amber BOOSTER_DUE badges within 14 days"
    - "Registering a new animal offline writes to local_animals and queues ANIMAL_REGISTRATION into offline_sync_queue"
  artifacts:
    - "mobile/src/services/animalService.ts"
    - "mobile/src/components/animals/AnimalCard.tsx"
    - "mobile/src/components/animals/VaccinationTimeline.tsx"
    - "mobile/src/components/animals/NewAnimalModal.tsx"
    - "mobile/src/views/AnimalRegistryView.tsx"
    - "mobile/src/tests/animalService.test.ts"
    - "mobile/src/tests/AnimalRegistryView.test.tsx"
  key_links:
    - "animalService interacts with dbService and syncEngineService"
    - "AnimalRegistryView renders AnimalCard and NewAnimalModal"
---

<objective>
Build the mobile offline digital cattle passbook and registry interface: `animalService.ts` for SQLite `local_animals` caching and DAHD booster calculation, `AnimalCard.tsx` and `VaccinationTimeline.tsx` for visual health histories, `NewAnimalModal.tsx` for offline registration, and update `AnimalRegistryView.tsx` with full 3-segment passbook navigation, verified by automated tests.

Purpose: Empowers farmers and field veterinarians with instant offline cattle passports and vaccination reminders.
Output: Integrated mobile animal passbook, vaccination tracker, and verified test suites.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-livestock-registry-pashu-aadhaar-cloud-persistence/06-CONTEXT.md
@.planning/phases/06-livestock-registry-pashu-aadhaar-cloud-persistence/06-UI-SPEC.md
@.planning/phases/06-livestock-registry-pashu-aadhaar-cloud-persistence/06-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-04 | Offline Lockout | animalService.ts | Farmer unable to view cattle passbook when offline in a barn | Cache all registered cattle in SQLite `local_animals` and seed realistic demo records for Ahmednagar. |
| T-06-05 | Input Error | NewAnimalModal.tsx | Invalid or incomplete tag number registered in the field | Enforce 12-digit numeric validation pattern and format automatically with hyphens (XXXX-XXXX-XXXX). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build animalService & Unit Tests</name>
  <files>mobile/src/services/animalService.ts, mobile/src/tests/animalService.test.ts</files>
  <read_first>mobile/src/database/sqliteConnection.ts, .planning/phases/06-livestock-registry-pashu-aadhaar-cloud-persistence/06-RESEARCH.md</read_first>
  <action>
    1. Create `mobile/src/services/animalService.ts`:
       - Types:
         - `LocalAnimal`: `{ tagNumber: string; ownerName: string; ownerMobileMasked: string; species: string; breed: string; ageMonths: number; villageLgdCode: number; villageName: string; vaccinationStatus: 'UP_TO_DATE' | 'BOOSTER_DUE' | 'OVERDUE'; lastSyncedAt: string; }`
         - `VaccineRecord`: `{ disease: 'FMD' | 'LSD' | 'ANTHRAX'; diseaseNameMarathi: string; lastDoseDate: string; nextBoosterDue: string; daysRemaining: number; status: 'UP_TO_DATE' | 'BOOSTER_DUE' | 'OVERDUE'; batchNumber: string; }`
       - Demo Seed Data:
         - Ramesh Patil's cattle in Rahuri (`558301`):
           1. Tag `100293847561`: Gir Cow, 36 months, FMD booster due in 8 days (`BOOSTER_DUE`).
           2. Tag `100293847562`: Murrah Buffalo, 48 months, all vaccines up to date (`UP_TO_DATE`).
       - Methods:
         - `seedDemoAnimalsIfEmpty(): Promise<void>`
         - `getAllAnimals(): Promise<LocalAnimal[]>`
         - `getAnimalByTag(tagNumber: string): Promise<LocalAnimal | null>`
         - `registerAnimal(data: Omit<LocalAnimal, 'lastSyncedAt'>): Promise<LocalAnimal>`:
           - Formats 12-digit tag, masks phone number.
           - Inserts into SQLite `local_animals`.
           - Enqueues `ANIMAL_REGISTRATION` payload into `offline_sync_queue`.
         - `getVaccinationSchedule(tagNumber: string): VaccineRecord[]`:
           - Evaluates FMD (180 days), LSD (365 days), and Anthrax (365 days).
    2. Create `mobile/src/tests/animalService.test.ts`:
       - Tests seeding and retrieving local animals.
       - Tests looking up animal by 12-digit tag.
       - Tests registering new animal and verifying SQLite insertion and sync queue entry.
       - Tests vaccination schedule calculates booster status correctly.
  </action>
  <verify>
    Run `npm --prefix mobile test -- src/tests/animalService.test.ts` to verify offline animal service.
  </verify>
  <acceptance_criteria>
    - `mobile/src/services/animalService.ts` exports `animalService`
    - `npm --prefix mobile test -- src/tests/animalService.test.ts` passes with code 0
  </acceptance_criteria>
  <done>
    Offline animal service implemented with SQLite queries, seed data, and tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AnimalCard, VaccinationTimeline, NewAnimalModal & Passbook UI</name>
  <files>mobile/src/components/animals/AnimalCard.tsx, mobile/src/components/animals/VaccinationTimeline.tsx, mobile/src/components/animals/NewAnimalModal.tsx, mobile/src/views/AnimalRegistryView.tsx, mobile/src/tests/AnimalRegistryView.test.tsx</files>
  <read_first>mobile/src/views/AnimalRegistryView.tsx, .planning/phases/06-livestock-registry-pashu-aadhaar-cloud-persistence/06-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/components/animals/AnimalCard.tsx`:
       - Renders cattle passport card with:
         - Species icon (Cow / Buffalo / Goat).
         - 12-digit tag number formatted as `XXXX-XXXX-XXXX`.
         - Breed and age in months / years.
         - Status badge: `✓ लस पूर्ण (Up to Date)` or `⚠️ लस देण्याची वेळ (Booster Due)`.
         - Owner name and village name.
    2. Create `mobile/src/components/animals/VaccinationTimeline.tsx`:
       - Renders DAHD vaccination ledger for FMD, LSD, and Anthrax.
       - Shows last dose date, next booster date, and countdown pills.
    3. Create `mobile/src/components/animals/NewAnimalModal.tsx`:
       - Modal using `FluidDrawer` with 52px thumb targets.
       - Fields: 12-digit Tag Number, Species selector, Breed, Age in months, Owner Name, Owner Mobile.
       - Calls `animalService.registerAnimal` and triggers tactile feedback.
    4. Update `mobile/src/views/AnimalRegistryView.tsx`:
       - Segmented controller:
         1. `माझे पशु (My Cattle)`: Grid of `AnimalCard` items for the registered owner.
         2. `पशु आधार शोध (Tag Lookup)`: 12-digit search input with quick-select demo tags, showing full animal card and `VaccinationTimeline`.
       - Top action button: `[ + नवीन पशू नोंदणी (New Cattle) ]` opening `NewAnimalModal`.
    5. Create `mobile/src/tests/AnimalRegistryView.test.tsx`:
       - Tests rendering "माझे पशु" list with seeded cattle.
       - Tests searching by 12-digit tag reveals animal profile and vaccination timeline.
       - Tests opening new animal modal and submitting new animal.
  </action>
  <verify>
    Run `npm --prefix mobile test -- src/tests/AnimalRegistryView.test.tsx` to verify passbook UI.
  </verify>
  <acceptance_criteria>
    - `mobile/src/views/AnimalRegistryView.tsx` features segmented passbook with tag lookup and registration
    - All tests in `mobile/src/tests/AnimalRegistryView.test.tsx` pass
    - Full test suite `npm --prefix mobile test -- --run` exits with code 0
  </acceptance_criteria>
  <done>
    Digital cattle passbook UI, vaccination timeline, and new animal registration modal verified.
  </done>
</task>

</tasks>
