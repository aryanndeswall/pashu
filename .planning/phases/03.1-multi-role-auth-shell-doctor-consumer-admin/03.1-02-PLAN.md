---
phase: 03.1-multi-role-auth-shell-doctor-consumer-admin
plan: 02
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - mobile/src/components/common/HeaderBar.tsx
  - mobile/src/components/navigation/BottomBar.tsx
  - mobile/src/store/navigationStore.ts
  - mobile/src/App.tsx
  - mobile/src/tests/roleNavigation.test.tsx
autonomous: true
requirements:
  - APK-03
  - AUTH-01
user_setup: []

must_haves:
  truths:
    - "Header bar renders an interactive role pill badge (e.g. 👨‍🌾 पशुपालक) with 1-tap demo switcher modal"
    - "Bottom navigation tabs dynamically filter based on active role (Consumer: 3 tabs; Doctor: 4 tabs; Admin: 4 tabs with Dashboard default)"
    - "Switching to Consumer role while on Labs tab automatically redirects to Report tab to prevent unauthorized access"
  artifacts:
    - "mobile/src/components/common/HeaderBar.tsx"
    - "mobile/src/components/navigation/BottomBar.tsx"
    - "mobile/src/tests/roleNavigation.test.tsx"
  key_links:
    - "HeaderBar.tsx reads authStore.activeRole and opens FluidDrawer with 1-tap persona swapping"
    - "BottomBar.tsx filters TAB_ITEMS using activeRole from authStore"
    - "navigationStore.ts redirects tab when restricted role is activated"
---

<objective>
Integrate the SIH 1-click Demo Role Switcher into `HeaderBar.tsx`, dynamically filter bottom navigation tabs in `BottomBar.tsx` based on the active persona, configure automatic tab redirect in `navigationStore.ts`, and create integration tests verifying role-based ergonomics and presentation workflows.

Purpose: Enables instant 1-click persona switching during live 5-minute SIH hackathon presentations and restricts UI features to authorized roles.
Output: Integrated header role switcher, role-filtered navigation, and automated verification tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-CONTEXT.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-UI-SPEC.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03.1-03 | Usability / Demo Friction | HeaderBar | Complicated logout/login procedures causing presenter delays during fast 5-minute hackathon jury evaluation | Implement 1-tap demo role switcher pill in the persistent header allowing instant persona swap. |
| T-03.1-04 | Authorization Bypass | Navigation | Farmer accessing clinical Lab Requisition Form (e-LRF) by preserving activeTab state from a previous session | Automatically validate tab against active role's permission matrix and redirect to `'report'` if unauthorized. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add SIH 1-Click Demo Role Switcher to HeaderBar</name>
  <files>mobile/src/components/common/HeaderBar.tsx</files>
  <read_first>mobile/src/store/authStore.ts, mobile/src/components/animations/FluidDrawer.tsx, .planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-UI-SPEC.md</read_first>
  <action>
    1. In `mobile/src/components/common/HeaderBar.tsx`:
       - Import `useAuthStore` and `DEMO_PERSONAS`.
       - Add state `isRoleDrawerOpen: boolean` (default: `false`).
       - In the header actions area (next to sunlight theme toggle):
         - Render an interactive Role Pill Badge:
           - Consumer: Emerald badge `👨‍🌾 पशुपालक (Farmer)`
           - Doctor: Blue badge `🩺 पशुवैद्य (Vet)`
           - Admin: Purple badge `🏛️ अधिकारी (Admin)`
         - On pill tap: calls `hapticsService.hapticLight()` and sets `isRoleDrawerOpen(true)`.
       - Render a `FluidDrawer` titled **"डेमो भूमिका बदला (SIH Demo Role Switcher)"**:
         - Lists all 3 personas (`consumer`, `doctor`, `admin`) with their bilingual titles, names, and assigned districts.
         - Shows a clear `✓ सध्या सक्रिय (Active)` tag for current role.
         - Single-tap button on any persona calls `switchRole(role)` and closes the drawer with haptic feedback.
  </action>
  <verify>
    Run `npm --prefix mobile run build` to verify HeaderBar TypeScript types and compilation.
  </verify>
  <acceptance_criteria>
    - `mobile/src/components/common/HeaderBar.tsx` displays role pill badge corresponding to `activeRole`
    - Tapping pill opens `FluidDrawer` with 3 1-tap role switch options
  </acceptance_criteria>
  <done>
    1-click demo role switcher integrated into app header.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Tab Filtering in BottomBar & Integration Tests</name>
  <files>mobile/src/components/navigation/BottomBar.tsx, mobile/src/store/navigationStore.ts, mobile/src/App.tsx, mobile/src/tests/roleNavigation.test.tsx</files>
  <read_first>mobile/src/store/authStore.ts, mobile/src/components/navigation/BottomBar.tsx, .planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-RESEARCH.md</read_first>
  <action>
    1. In `mobile/src/components/navigation/BottomBar.tsx`:
       - Read `activeRole` from `useAuthStore`.
       - Filter available tabs:
         - `consumer`: Report (नोंदणी), Dashboard (डॅशबोर्ड), Animals (माझे पशु). *Labs tab hidden.*
         - `doctor`: Report (८ लक्षणे), Dashboard (डॅशबोर्ड), Animals (पशु आधार), Labs (प्रयोगशाळा).
         - `admin`: Dashboard (कमांड सेंटर), Report (क्लिनिकल सारांश), Animals (नोंदणी), Labs (लॅब पडताळणी).
       - Maintain the center elevated SOS button with role-tailored label:
         - Consumer: Helpline / Help Sakhi
         - Doctor: Anthrax Biohazard SOS
         - Admin: Outbreak Containment Broadcast
    2. In `mobile/src/store/navigationStore.ts`:
       - Add guard `validateTabForRole(role: UserRole)`: if role is `'consumer'` and `activeTab === 'labs'`, automatically set `activeTab = 'report'`.
    3. In `mobile/src/App.tsx`:
       - Initialize `useAuthStore.getState().initSession()` on mount to restore SQLite session.
       - Display a subtle welcome banner under HeaderBar:
         - e.g. `👨‍🌾 स्वागत आहे, रमेश पाटील (राहुरी खुर्द)` with role indicator.
    4. Create `mobile/src/tests/roleNavigation.test.tsx`:
       - Tests that when `activeRole === 'consumer'`, the `labs` tab is not present in the DOM.
       - Tests that when `activeRole === 'doctor'`, all 4 tabs (Report, Dashboard, Animals, Labs) are rendered.
       - Tests switching from Doctor to Consumer redirects activeTab from `'labs'` to `'report'`.
  </action>
  <verify>
    Run `npm --prefix mobile test -- --run` and confirm all 31+ tests pass with 100% green assertions.
  </verify>
  <acceptance_criteria>
    - `mobile/src/components/navigation/BottomBar.tsx` dynamically hides Labs tab for Consumer role
    - `mobile/src/tests/roleNavigation.test.tsx` passes with 100% green assertions
    - `npm --prefix mobile test -- --run` passes completely
  </acceptance_criteria>
  <done>
    Dynamic role-based navigation and safety redirection implemented and verified.
  </done>
</task>

</tasks>
