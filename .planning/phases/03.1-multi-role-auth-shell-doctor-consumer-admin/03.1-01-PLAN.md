---
phase: 03.1-multi-role-auth-shell-doctor-consumer-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/database/migrations.ts
  - mobile/src/database/sqliteConnection.ts
  - mobile/src/store/authStore.ts
  - mobile/src/views/RoleSelectionView.tsx
  - mobile/src/tests/authStore.test.ts
  - mobile/src/tests/RoleSelection.test.tsx
autonomous: true
requirements:
  - APK-03
  - AUTH-01
user_setup: []

must_haves:
  truths:
    - "Auth store manages activeRole ('consumer' | 'doctor' | 'admin') with default 'consumer' and seeded persona profiles"
    - "SQLite schema contains auth_session table persisting active persona across offline reboots"
    - "RoleSelectionView presents 3 high-contrast persona cards with 52px touch targets and Devanagari descriptions"
  artifacts:
    - "mobile/src/store/authStore.ts"
    - "mobile/src/views/RoleSelectionView.tsx"
    - "mobile/src/tests/authStore.test.ts"
    - "mobile/src/tests/RoleSelection.test.tsx"
  key_links:
    - "authStore.ts persists session changes to SQLite dbService and triggers hapticsService.hapticLight"
    - "RoleSelectionView.tsx invokes authStore.switchRole upon card selection"
---

<objective>
Build the core multi-role authorization state in `authStore.ts` with seeded demo personas (Farmer, Vet, Admin), create the SQLite `auth_session` migration table for offline durability, implement the dedicated `RoleSelectionView.tsx` screen, and verify with automated unit tests.

Purpose: Establishes the 3-role identity foundation supporting livestock owners, field veterinarians, and district administrators.
Output: Tested auth store and role selection view with SQLite session persistence.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-CONTEXT.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-RESEARCH.md
@.planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-UI-SPEC.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03.1-01 | State Loss | Auth Store | App termination or OS reboot resetting active role back to default and disrupting field workflows | Persist active role and profile to SQLite `auth_session` table on every role change, and restore upon boot. |
| T-03.1-02 | Input Validation | Role Selection | Invalid role token injected causing application crash or undefined access | Restrict `UserRole` to strict TypeScript union `'consumer' | 'doctor' | 'admin'` with fallback validation. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add auth_session SQLite Migration & Build useAuthStore</name>
  <files>mobile/src/database/migrations.ts, mobile/src/database/sqliteConnection.ts, mobile/src/store/authStore.ts, mobile/src/tests/authStore.test.ts</files>
  <read_first>mobile/src/database/migrations.ts, mobile/src/database/sqliteConnection.ts, .planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-RESEARCH.md</read_first>
  <action>
    1. In `mobile/src/database/migrations.ts`, add the `auth_session` DDL statement:
       ```sql
       CREATE TABLE IF NOT EXISTS auth_session (
         id TEXT PRIMARY KEY,
         active_role TEXT NOT NULL,
         display_name TEXT NOT NULL,
         district TEXT NOT NULL,
         block TEXT NOT NULL,
         updated_at TEXT NOT NULL
       );
       ```
    2. In `mobile/src/database/sqliteConnection.ts`, update in-memory fallback initialization to register `'auth_session'` table with default schema.
    3. In `mobile/src/store/authStore.ts`, implement `useAuthStore` using Zustand:
       - Types:
         - `UserRole = 'consumer' | 'doctor' | 'admin'`
         - `UserProfile = { id, name, nameMarathi, role, mobileNumberMasked, district, block, licenseOrId? }`
       - Export constant `DEMO_PERSONAS: Record<UserRole, UserProfile>`:
         - `consumer`: Ramesh Patil, Rahuri Khurd, +91 9822X-XX412
         - `doctor`: Dr. Anjali Deshmukh, Rahuri & Sangamner, MH-VET-8819
         - `admin`: Dr. S. K. Kulkarni (DVO), Ahmednagar District, DVO-AHM-001
       - State & Actions:
         - `activeRole: UserRole` (default: `'consumer'`)
         - `userProfile: UserProfile` (default: `DEMO_PERSONAS.consumer`)
         - `switchRole(role: UserRole): Promise<void>`:
           - Sets activeRole and userProfile.
           - Emits `hapticsService.hapticLight()`.
           - Upserts record to SQLite table `auth_session`.
         - `initSession(): Promise<void>`: Loads last persisted role from SQLite `auth_session`.
    4. Create `mobile/src/tests/authStore.test.ts`:
       - Tests default role is `'consumer'`.
       - Tests switching to `'doctor'` and `'admin'` updates profile and role.
       - Tests session persistence write to SQLite.
  </action>
  <verify>
    Run `npm --prefix mobile test -- --run src/tests/authStore.test.ts` to confirm store and persistence tests pass.
  </verify>
  <acceptance_criteria>
    - `mobile/src/database/migrations.ts` contains `auth_session` table DDL
    - `mobile/src/store/authStore.ts` exports `useAuthStore` and `DEMO_PERSONAS`
    - `npm --prefix mobile test -- --run src/tests/authStore.test.ts` exits with code 0
  </acceptance_criteria>
  <done>
    Auth store with SQLite offline session persistence and seeded demo personas implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Dedicated RoleSelectionView with 3 Persona Cards</name>
  <files>mobile/src/views/RoleSelectionView.tsx, mobile/src/tests/RoleSelection.test.tsx</files>
  <read_first>mobile/src/store/authStore.ts, .planning/phases/03.1-multi-role-auth-shell-doctor-consumer-admin/03.1-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/views/RoleSelectionView.tsx`:
       - Displays app title header with shield icon and title: "पशु सुरक्षा — भूमिका निवडा (Select Role)".
       - Subtitle: "तुमची दैनंदिन कार्यप्रणाली सुरू करण्यासाठी भूमिका निवडा".
       - 3 High-Contrast Persona Selection Cards (`.field-touch-target` 52px+):
         - **Card 1: पशुपालक (Farmer / Consumer)**
           - Icon: 👨‍🌾 / `UserCheck`, Accent: Emerald (`#059669`).
           - Copy: "लक्षणे नोंदवा, जनावरांचा पशू आधार तपासा आणि थेट पशु सखीशी संपर्क साधा."
           - Active pill: `✓ सक्रिय भूमिका (Active Role)` if currently selected.
           - CTA button: `हा रोल निवडा (Select Farmer)`.
         - **Card 2: पशुवैद्य (Veterinarian / Doctor)**
           - Icon: 🩺 / `Stethoscope`, Accent: Blue (`#2563eb`).
           - Copy: "८-लक्षणे क्लिनिकल ट्रायज, ॲन्थ्रॅक्स लॉकआऊट, e-LRF लॅब नमुना नोंदणी."
           - CTA button: `हा रोल निवडा (Select Doctor)`.
         - **Card 3: जिल्हा अधिकारी (District Admin / DVO)**
           - Icon: 🏛️ / `ShieldAlert`, Accent: Purple (`#7c3aed`).
           - Copy: "थेट उद्रेक नियंत्रण, १-५-१० किमी क्षेत्र सीमा, आणि बाजार बंदी आदेश."
           - CTA button: `हा रोल निवडा (Select Admin)`.
       - On selecting a role: calls `switchRole(role)` and optional `onRoleSelected` callback.
    2. Create `mobile/src/tests/RoleSelection.test.tsx`:
       - Tests rendering of all 3 persona cards with Devanagari labels.
       - Tests clicking a role card triggers `switchRole` with correct role identifier.
       - Tests visual active state reflects current `activeRole`.
  </action>
  <verify>
    Run `npm --prefix mobile test -- --run src/tests/RoleSelection.test.tsx` to verify component interactions.
  </verify>
  <acceptance_criteria>
    - `mobile/src/views/RoleSelectionView.tsx` renders 3 persona cards with bilingual labels and 52px buttons
    - `mobile/src/tests/RoleSelection.test.tsx` passes with 100% green assertions
  </acceptance_criteria>
  <done>
    Dedicated role selection view implemented and validated.
  </done>
</task>

</tasks>
