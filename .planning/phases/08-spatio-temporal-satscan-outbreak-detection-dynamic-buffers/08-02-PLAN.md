---
phase: 08-spatio-temporal-satscan-outbreak-detection-dynamic-buffers
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - backend/app/services/buffer_service.py
  - backend/app/api/v1/clusters.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_buffers.py
autonomous: true
requirements:
  - GEO-03
user_setup: []

must_haves:
  truths:
    - "Dynamic containment buffer generator creates 1km (Infected Zone), 5km (Ring Vaccination Zone), and 10km (Surveillance Perimeter) concentric geodetic polygons"
    - "Buffer output strictly complies with GeoJSON FeatureCollection format ready for MapLibre GL JS / Deck.gl"
    - "FastAPI exposes POST /api/v1/clusters/evaluate and GET /api/v1/clusters/{cluster_id}/buffers"
  artifacts:
    - "backend/app/services/buffer_service.py"
    - "backend/app/api/v1/clusters.py"
    - "backend/tests/test_buffers.py"
  key_links:
    - "buffer_service generates closed 64-vertex circular polygons around cluster epicenters"
    - "clusters API router mounts under /api/v1/clusters"
---

<objective>
Implement the dynamic 1km / 5km / 10km containment buffer generator (`buffer_service.py`), expose the cluster evaluation and GeoJSON buffer retrieval endpoints on FastAPI (`backend/app/api/v1/clusters.py`), and verify with automated pytest suites.

Purpose: Transforms statistical cluster detections into actionable, cartographic biosecurity zones for veterinary command and quarantine enforcement.
Output: Tested buffer generator, GeoJSON serialization, and cluster API endpoints.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-spatio-temporal-satscan-outbreak-detection-dynamic-buffers/08-CONTEXT.md
@.planning/phases/08-spatio-temporal-satscan-outbreak-detection-dynamic-buffers/08-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-03 | Planar Distortion | buffer_service.py | Distorted circular buffers at high/low latitudes | Account for cosine of latitude when computing longitudinal offsets in geodesic circle generation. |
| T-08-04 | Malformed GeoJSON | clusters.py | Front-end GIS crashes due to unclosed polygon coordinates | Enforce first coordinate == last coordinate in all generated polygon rings. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build Geodesic Buffer Generator Service</name>
  <files>backend/app/services/buffer_service.py</files>
  <read_first>.planning/phases/08-spatio-temporal-satscan-outbreak-detection-dynamic-buffers/08-RESEARCH.md</read_first>
  <action>
    1. Create `backend/app/services/buffer_service.py`:
       - `generate_geodesic_circle(lat: float, lon: float, radius_meters: float, num_points: int = 64) -> List[List[float]]`:
         - Calculates closed polygon ring `[[lon, lat], ... [lon, lat]]`.
         - Accounts for `cos(lat)` to avoid non-equatorial longitudinal distortion.
       - `generate_containment_buffers(center_lat: float, center_lon: float, cluster_id: str) -> Dict[str, Any]`:
         - Creates 3 concentric buffer rings:
           1. `INFECTED_ZONE` (1,000m / 1.0 km): Red `#dc2626`, Action: "Complete Movement Freeze & Clinical Search"
           2. `RING_VACCINATION_ZONE` (5,000m / 5.0 km): Amber `#f59e0b`, Action: "72-Hour Emergency Blanket Ring Vaccination"
           3. `SURVEILLANCE_ZONE` (10,000m / 10.0 km): Cyan `#06b6d4`, Action: "Market (Haat) Closures & Highway Check-Posts"
         - Returns GeoJSON `FeatureCollection` with styled properties for MapLibre / Deck.gl.
  </action>
  <verify>
    Run `python -c "from app.services.buffer_service import generate_containment_buffers; print(generate_containment_buffers(19.3912, 74.6521, 'CL-1'))"`
  </verify>
  <acceptance_criteria>
    - `buffer_service.py` generates compliant GeoJSON FeatureCollection with 3 polygon zones
    - Generated coordinates form closed rings
  </acceptance_criteria>
  <done>
    Geodesic containment buffer service implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Clusters API Router & Automated Pytest Suite</name>
  <files>backend/app/api/v1/clusters.py, backend/app/api/v1/__init__.py, backend/tests/test_buffers.py</files>
  <read_first>backend/app/services/buffer_service.py, backend/app/services/satscan_service.py, backend/app/api/v1/__init__.py</read_first>
  <action>
    1. Create `backend/app/api/v1/clusters.py`:
       - `router = APIRouter()`
       - In-memory store for active clusters across demo sessions.
       - `POST /api/v1/clusters/evaluate`: Ingests `ClusterEvaluationRequest`, runs SaTScan evaluation via `satscan_service`, stores cluster if `OUTBREAK_DECLARED`, and returns `ClusterEvaluationResponse`.
       - `GET /api/v1/clusters/active`: Returns list of active outbreak clusters.
       - `GET /api/v1/clusters/{cluster_id}/buffers`: Returns GeoJSON `FeatureCollection` generated by `buffer_service`.
    2. Mount `clusters_router` onto `backend/app/api/v1/__init__.py` under prefix `"/clusters"`.
    3. Create `backend/tests/test_buffers.py`:
       - Tests `generate_containment_buffers` returns 3 GeoJSON polygon features.
       - Tests closed ring polygon coordinate validation.
       - Tests `POST /api/v1/clusters/evaluate` endpoint.
       - Tests `GET /api/v1/clusters/active` endpoint.
       - Tests `GET /api/v1/clusters/{cluster_id}/buffers` endpoint returns GeoJSON FeatureCollection.
  </action>
  <verify>
    Run `python -m pytest backend/tests/test_buffers.py -v` to verify all tests pass.
  </verify>
  <acceptance_criteria>
    - Clusters router mounted under `/api/v1/clusters`
    - All tests in `backend/tests/test_buffers.py` pass 100%
  </acceptance_criteria>
  <done>
    Cluster endpoints, buffer GeoJSON serialization, and pytests verified.
  </done>
</task>

</tasks>
