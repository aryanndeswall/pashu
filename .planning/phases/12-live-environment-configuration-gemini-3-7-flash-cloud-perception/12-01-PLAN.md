---
phase: 12-live-environment-configuration-gemini-3-7-flash-cloud-perception
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/app/config.py
  - backend/app/services/triage_service.py
  - backend/tests/test_live_gemini.py
  - mobile/.env
  - mobile/src/config/api.ts
  - mobile/src/tests/apiConfig.test.ts
autonomous: true
requirements:
  - CLOUD-01
  - CLOUD-03
user_setup: []

must_haves:
  truths:
    - "Backend boots with .env configuration and loads GEMINI_API_KEY and GEMINI_MODEL"
    - "Gemini client initializes successfully when GEMINI_API_KEY is present and falls back gracefully when absent"
    - "Mobile client provides centralized dynamic API gateway resolution with default fallback to http://localhost:8000/api/v1"
    - "Automated unit tests verify API configuration and mock/live Gemini perception"
  artifacts:
    - "backend/.env"
    - "mobile/.env"
    - "mobile/src/config/api.ts"
    - "mobile/src/tests/apiConfig.test.ts"
    - "backend/tests/test_live_gemini.py"
  key_links:
    - "backend/app/config.py loads backend/.env for GEMINI_API_KEY and GEMINI_MODEL"
    - "mobile/src/config/api.ts reads import.meta.env.VITE_API_BASE_URL"
---

<objective>
Configure backend and mobile environment templates, establish the centralized mobile dynamic API gateway service, and create the Gemini live verification test suite to validate both live and fallback inference states.

Purpose: Lays the environment and networking foundation for connecting Pashu-Suraksha to live cloud services while maintaining zero-breakage resilience in offline dead zones.
Output: Backend `.env`, mobile `.env`, dynamic API gateway module `mobile/src/config/api.ts`, and test suites `backend/tests/test_live_gemini.py` and `mobile/src/tests/apiConfig.test.ts`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-CONTEXT.md
@.planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-RESEARCH.md
@.planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-VALIDATION.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-01 | Credential Exposure | backend/.env, mobile/.env | Cloud API secrets committed into git repo | Ensure `.env` is ignored in `.gitignore`, provide commented `.env.example`, and never store `GEMINI_API_KEY` on mobile client. |
| T-12-02 | Silent Fallback Failure | backend/app/services/triage_service.py | Malformed key or missing environment file causes unhandled exception | Catch client initialization errors and route to `EdgeRulesEvaluator` fallback. |
| T-12-03 | Network Gateway Misconfiguration | mobile/src/config/api.ts | Invalid URL format (missing trailing/leading slashes) breaks API queries | URL sanitizer utility in `getApiUrl()` standardizes slash normalization. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Environment Files and Verify Git Exclusion</name>
  <files>backend/.env, mobile/.env, .gitignore</files>
  <read_first>backend/.env.example, mobile/.env.example, .gitignore</read_first>
  <action>
    1. Check `.gitignore` to ensure `.env` and `.env.*` (except `.env.example`) are safely ignored.
    2. Create `backend/.env` from `backend/.env.example` with default settings (`GEMINI_MODEL="gemini-2.5-flash"`).
    3. Create `mobile/.env` from `mobile/.env.example` with `VITE_API_BASE_URL="http://localhost:8000/api/v1"`.
  </action>
  <verify>
    Run `git status` to verify `backend/.env` and `mobile/.env` are ignored by git.
  </verify>
  <acceptance_criteria>
    - `backend/.env` exists and contains `GEMINI_MODEL="gemini-2.5-flash"`
    - `mobile/.env` exists and contains `VITE_API_BASE_URL="http://localhost:8000/api/v1"`
    - Neither file is tracked by git
  </acceptance_criteria>
  <done>
    Environment configuration files instantiated safely with zero git tracking leakage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Centralized Dynamic API Gateway Module & Vitest Unit Tests</name>
  <files>mobile/src/config/api.ts, mobile/src/tests/apiConfig.test.ts</files>
  <read_first>mobile/src/services/aiTriageService.ts, .planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-RESEARCH.md</read_first>
  <action>
    1. Create `mobile/src/config/api.ts`:
       - Export `API_CONFIG` object with `baseUrl` (reading `import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000/api/v1'`) and `timeoutMs` (default 2500).
       - Export helper `getApiUrl(endpoint: string): string` that safely strips extra slashes.
       - Export helper `getTriageEndpoint(): string` returning `${API_CONFIG.baseUrl}/triage/multimodal`.
    2. Create `mobile/src/tests/apiConfig.test.ts`:
       - Test default baseUrl resolution.
       - Test URL path joining without double slashes.
       - Test timeoutMs configuration.
  </action>
  <verify>
    Run `npm test -- apiConfig.test.ts` in `mobile/`.
  </verify>
  <acceptance_criteria>
    - `mobile/src/config/api.ts` exports `API_CONFIG`, `getApiUrl`, and `getTriageEndpoint`
    - `mobile/src/tests/apiConfig.test.ts` passes 100%
  </acceptance_criteria>
  <done>
    Centralized mobile API gateway module created and verified with unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Backend Live Gemini Verification Suite</name>
  <files>backend/tests/test_live_gemini.py, backend/app/services/triage_service.py</files>
  <read_first>backend/app/config.py, backend/app/services/triage_service.py, backend/tests/test_triage.py</read_first>
  <action>
    1. Review `backend/app/services/triage_service.py` to ensure `GeminiTriageService` properly handles missing or empty `GEMINI_API_KEY` (sets `self.client = None`).
    2. Create `backend/tests/test_live_gemini.py`:
       - `test_gemini_config_defaults`: Verifies `settings.GEMINI_MODEL == "gemini-2.5-flash"`.
       - `test_gemini_service_fallback_without_api_key`: Verifies that without API key, triage returns valid `TriageResponse` with `model_used="EdgeRulesEvaluator-Default"` or appropriate rule.
       - `test_gemini_mocked_live_call`: Mocks `genai.Client` to simulate successful Google GenAI response adhering to schema.
       - `test_gemini_live_call_when_key_set`: Checks `os.environ.get("GEMINI_API_KEY")`; if present, executes a live call with a real clinical prompt ("bovine with cutaneous nodules and high fever") and asserts schema adherence and <800ms latency. If absent, skips cleanly using `pytest.mark.skipif`.
  </action>
  <verify>
    Run `pytest backend/tests/test_live_gemini.py` with both empty key and simulated key.
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_live_gemini.py` passes all non-skipped tests cleanly
    - Zero regressions across existing backend tests
  </acceptance_criteria>
  <done>
    Backend live Gemini verification suite created with dual-mode support (mock/live).
  </done>
</task>

</tasks>
