---
phase: 12-live-environment-configuration-gemini-3-7-flash-cloud-perception
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - mobile/src/services/aiTriageService.ts
  - mobile/src/services/syncEngineService.ts
  - mobile/src/tests/aiTriageFallback.test.ts
  - backend/tests/test_triage.py
autonomous: true
requirements:
  - CLOUD-01
  - CLOUD-03
user_setup: []

must_haves:
  truths:
    - "aiTriageService uses centralized API_CONFIG and getApiUrl rather than hardcoding localhost"
    - "syncEngineService uses centralized API_CONFIG for endpoint resolution"
    - "aiTriageService aborts network calls exceeding timeout (2500ms) and smoothly falls back to on-device heuristic engine"
    - "Automated tests in mobile and backend verify end-to-end multimodal perception flow and latency limits"
  artifacts:
    - "mobile/src/services/aiTriageService.ts"
    - "mobile/src/services/syncEngineService.ts"
    - "mobile/src/tests/aiTriageFallback.test.ts"
  key_links:
    - "aiTriageService imports getApiUrl and API_CONFIG from @/config/api"
    - "syncEngineService imports getApiUrl from @/config/api"
---

<objective>
Wire the mobile application services to the dynamic API gateway, integrate the abort timeout and resilient on-device heuristic fallback, and verify end-to-end multimodal triage execution and latency benchmarking across the mobile and backend stacks.

Purpose: Completes the full loop from field mobile device to cloud perception gateway, ensuring uninterrupted triage even in cellular dead zones.
Output: Refactored mobile services, unit test suite for offline fallback, and verified full project test suite pass.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-CONTEXT.md
@.planning/phases/12-live-environment-configuration-gemini-3-7-flash-cloud-perception/12-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-04 | App Hang on Poor Cellular Connection | mobile/src/services/aiTriageService.ts | Poor rural cellular signal stalls HTTP fetch indefinitely | Enforce AbortController with configurable timeout (2500ms) that cleanly triggers on-device heuristic fallback. |
| T-12-05 | Stale Endpoint Ingestion | mobile/src/services/syncEngineService.ts | Hardcoded URLs prevent directing sync queue to cloud endpoints | Route telemetry and media sync endpoints through `getApiUrl()`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire Dynamic API Gateway into Mobile Services</name>
  <files>mobile/src/services/aiTriageService.ts, mobile/src/services/syncEngineService.ts</files>
  <read_first>mobile/src/config/api.ts, mobile/src/services/aiTriageService.ts, mobile/src/services/syncEngineService.ts</read_first>
  <action>
    1. Update `mobile/src/services/aiTriageService.ts`:
       - Import `API_CONFIG`, `getApiUrl` from `../config/api`.
       - Replace hardcoded `apiBaseUrl = 'http://localhost:8000/api/v1/triage'` with dynamic resolution:
         `private getTriageUrl(): string { return getApiUrl('triage/multimodal'); }`
       - Use `API_CONFIG.timeoutMs` for the abort controller timeout.
       - Ensure catch block logs non-intrusively and calls `evaluateOnDevice`.
    2. Update `mobile/src/services/syncEngineService.ts`:
       - Import `getApiUrl` from `../config/api`.
       - Update endpoint resolutions to use `getApiUrl('sync/telemetry')` and `getApiUrl('sync/media')`.
  </action>
  <verify>
    Run `npm run build` in `mobile/` to confirm zero TypeScript compilation errors.
  </verify>
  <acceptance_criteria>
    - No hardcoded `http://localhost:8000` strings remain in `mobile/src/services/`
    - TypeScript compiles with 0 errors
  </acceptance_criteria>
  <done>
    Mobile services wired to centralized dynamic API gateway.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Unit Tests for Network Fallback & Timeout</name>
  <files>mobile/src/tests/aiTriageFallback.test.ts</files>
  <read_first>mobile/src/services/aiTriageService.ts, mobile/src/config/api.ts</read_first>
  <action>
    1. Create `mobile/src/tests/aiTriageFallback.test.ts`:
       - Mock `fetch` to simulate server errors (HTTP 500, network rejection, timeout abort).
       - Verify that `aiTriageService.runMultimodalTriage()` falls back to on-device heuristic engine without throwing.
       - Verify that Anthrax symptoms trigger instant `CRITICAL_ANTHRAX_LOCK` on-device without making network calls.
       - Verify that valid mock HTTP response returns cloud triage data.
  </action>
  <verify>
    Run `npm test -- aiTriageFallback.test.ts` in `mobile/`.
  </verify>
  <acceptance_criteria>
    - `mobile/src/tests/aiTriageFallback.test.ts` passes 100%
    - Both cloud success and offline fallback flows are validated
  </acceptance_criteria>
  <done>
    Network fallback and timeout handling verified with automated tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run Full Project Regression & Latency Verification</name>
  <files>backend/tests/test_triage.py, backend/tests/test_live_gemini.py</files>
  <read_first>backend/tests/test_triage.py, backend/tests/test_live_gemini.py</read_first>
  <action>
    1. Run full backend pytest suite: `pytest backend/tests/` to verify all existing and new tests pass.
    2. Run full mobile vitest suite: `npm test -- --run` in `mobile/` to verify all frontend tests pass.
    3. Run mobile production bundle build: `npm run build` in `mobile/`.
  </action>
  <verify>
    Confirm 0 failures in pytest, 0 failures in vitest, and successful Vite production bundle generation.
  </verify>
  <acceptance_criteria>
    - Full pytest suite passes
    - Full vitest suite passes
    - `npm run build` completes cleanly
  </acceptance_criteria>
  <done>
    End-to-end multimodal perception and mobile dynamic gateway verified across the entire system.
  </done>
</task>

</tasks>
