---
phase: 02-mobile-ui-design-system-react-bits-micro-interactions
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - mobile/src/types/syndromes.ts
  - mobile/src/components/animations/RadarSweep.tsx
  - mobile/src/components/animations/HazardBorder.tsx
  - mobile/src/components/animations/CountUpTicker.tsx
  - mobile/src/components/animations/FluidDrawer.tsx
  - mobile/src/components/syndromes/AnatomicalBadge.tsx
  - mobile/src/components/syndromes/SyndromeCard.tsx
  - mobile/src/components/syndromes/SyndromeGrid.tsx
  - mobile/src/components/modals/EmergencySOSModal.tsx
  - mobile/src/views/ReportView.tsx
  - mobile/src/views/DashboardView.tsx
  - mobile/src/views/AnimalRegistryView.tsx
  - mobile/src/views/LabReferralView.tsx
  - mobile/src/App.tsx
  - mobile/src/tests/syndromes.test.tsx
autonomous: true
requirements:
  - SYN-01
  - APK-03
user_setup: []

must_haves:
  truths:
    - "User can select from 8 standardized syndromic categories (VSS, NSLS, HSDS, AROS, CMSS, SARF, HES, NAS) via an icon-first visual selector"
    - "Hyperacute Sudden Death Syndrome (HSDS / Anthrax) renders with a pulsating crimson hazard border and triggers the emergency biohazard warning"
    - "React Bits animations (RadarSweep, HazardBorder, CountUpTicker, FluidDrawer) render smoothly using hardware-accelerated CSS transforms"
  artifacts:
    - "mobile/src/types/syndromes.ts"
    - "mobile/src/components/animations/RadarSweep.tsx"
    - "mobile/src/components/animations/HazardBorder.tsx"
    - "mobile/src/components/animations/CountUpTicker.tsx"
    - "mobile/src/components/animations/FluidDrawer.tsx"
    - "mobile/src/components/syndromes/AnatomicalBadge.tsx"
    - "mobile/src/components/syndromes/SyndromeCard.tsx"
    - "mobile/src/components/syndromes/SyndromeGrid.tsx"
    - "mobile/src/components/modals/EmergencySOSModal.tsx"
    - "mobile/src/views/ReportView.tsx"
    - "mobile/src/views/DashboardView.tsx"
    - "mobile/src/App.tsx"
    - "mobile/src/tests/syndromes.test.tsx"
  key_links:
    - "mobile/src/components/syndromes/SyndromeGrid.tsx maps over SYNDROME_TAXONOMY and renders SyndromeCards"
    - "mobile/src/components/syndromes/SyndromeCard.tsx applies HazardBorder when code is HSDS"
    - "mobile/src/App.tsx renders BottomBar and switches views based on navigationStore.activeTab"
---

<objective>
Implement the React Bits hardware-accelerated animation suite (RadarSweep, HazardBorder, CountUpTicker, FluidDrawer), build the 8-syndrome visual selector with anatomical badges and bilingual Marathi/Hindi subtitles, construct the emergency biohazard modal, and integrate the full 4-tab views inside `App.tsx`.

Purpose: Delivers the core visual syndromic triage interface (`SYN-01`) and interactive React Bits polish inside the APK container.
Output: Completed and tested 8-syndrome grid, React Bits animation components, and full application screen flow in `/mobile`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-CONTEXT.md
@.planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-UI-SPEC.md
@.planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-RESEARCH.md
@.planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-04 | Performance / DoS | WebView Animation | Complex CSS blur filters or layout reflows cause severe lag (<30 FPS) on budget Android devices | Implement React Bits animations using pure CSS `transform`, `opacity`, and `conic-gradient` with `will-change: transform`. |
| T-02-05 | Operational Error | Syndrome Selection | Low-literacy field workers misdiagnose diseases due to obscure clinical abbreviations | Render large anatomical vector badges (mouth/hoof, skin lumps, lungs, blood) and colloquial Marathi terms (*लाळ गळणे, गाठी, रक्ताची संडास*) alongside codes. |
| T-02-06 | Biohazard Exposure | Anthrax Lockout | Workers cutting an Anthrax-infected carcass causing human spore inhalation or fatal skin pustules | Display flashing crimson hazard border and prominent biohazard lockout modal ("शव विच्छेदन करू नका! / DO NOT CUT CARCASS") on HSDS selection. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define Syndrome Taxonomy & Build React Bits Animation Suite</name>
  <files>mobile/src/types/syndromes.ts, mobile/src/components/animations/RadarSweep.tsx, mobile/src/components/animations/HazardBorder.tsx, mobile/src/components/animations/CountUpTicker.tsx, mobile/src/components/animations/FluidDrawer.tsx</files>
  <read_first>.planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-CONTEXT.md, .planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/types/syndromes.ts`:
       - Syndrome union: `'VSS' | 'NSLS' | 'HSDS' | 'AROS' | 'CMSS' | 'SARF' | 'HES' | 'NAS'`.
       - Interface `SyndromeDefinition` with fields: `code`, `nameEnglish`, `nameMarathi`, `colloquialMarathi`, `anatomicalPart`, `severity: 'ROUTINE_ENDEMIC' | 'ELEVATED' | 'HIGH_CONTAGION' | 'CRITICAL_BIOHAZARD'`, `description`, `commonSuspects: string[]`.
       - Export constant `SYNDROME_TAXONOMY: SyndromeDefinition[]` covering all 8 syndromes specified in `02-UI-SPEC.md`.
    2. Create `mobile/src/components/animations/RadarSweep.tsx`:
       - 60 FPS CSS-based concentric expanding radar rings with a rotating conic-gradient sweep.
       - Visualizes local geodetic surveillance perimeters (5km / 10km scale) with zero remote map dependencies.
    3. Create `mobile/src/components/animations/HazardBorder.tsx`:
       - Alternating pulsating crimson glow border (`0 0 16px rgba(220, 38, 38, 0.6)`) at a 1.2s ease-in-out cycle.
       - Wraps HSDS card, alert banners, and biohazard emergency modal.
    4. Create `mobile/src/components/animations/CountUpTicker.tsx`:
       - High-performance `requestAnimationFrame` hook rolling up numbers smoothly over 800ms with ease-out cubic curve.
       - Formats integer roll-ups for queued offline reports and local case statistics.
    5. Create `mobile/src/components/animations/FluidDrawer.tsx`:
       - Hardware-accelerated CSS `translateY` spring bottom sheet drawer with touch swipe drag-down dismissal and accessible backdrop overlay.
  </action>
  <verify>
    Run `npm --prefix mobile run build` and confirm all 4 animation components compile with zero TypeScript errors.
  </verify>
  <acceptance_criteria>
    - `mobile/src/types/syndromes.ts` defines all 8 clinical syndromes with Marathi translations
    - `mobile/src/components/animations/RadarSweep.tsx` renders concentric circles and rotating gradient
    - `mobile/src/components/animations/HazardBorder.tsx` applies pulsating crimson glow
    - `mobile/src/components/animations/CountUpTicker.tsx` handles integer count animations
  </acceptance_criteria>
  <done>
    Syndromic taxonomy and React Bits animation suite implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 8-Syndrome Visual Selector, Anatomical Badges & Emergency SOS Modal</name>
  <files>mobile/src/components/syndromes/AnatomicalBadge.tsx, mobile/src/components/syndromes/SyndromeCard.tsx, mobile/src/components/syndromes/SyndromeGrid.tsx, mobile/src/components/modals/EmergencySOSModal.tsx, mobile/src/tests/syndromes.test.tsx</files>
  <read_first>mobile/src/types/syndromes.ts, mobile/src/services/hapticsService.ts, .planning/phases/02-mobile-ui-design-system-react-bits-micro-interactions/02-UI-SPEC.md</read_first>
  <action>
    1. Create `mobile/src/components/syndromes/AnatomicalBadge.tsx`:
       - Renders SVG icon badges indicating symptom regions:
         - Mouth & Hoof (`Footprints` / `Sparkles` / Smile for VSS)
         - Skin Lumps (`CircleDot` / `Grid` for NSLS)
         - Blood / Skull (`Skull` / `Droplet` for HSDS)
         - Lungs & Snout (`Wind` / `Activity` for AROS)
         - Swollen Quarter (`ShieldAlert` for CMSS)
         - Reproductive (`HeartPulse` for SARF)
         - Enteric / Diarrhea (`Waves` / `Flame` for HES)
         - Neurological (`Zap` / `RotateCcw` for NAS)
    2. Create `mobile/src/components/syndromes/SyndromeCard.tsx`:
       - Enforces `.field-touch-target` (52px+).
       - Features anatomical badge, syndrome code badge, bilingual Marathi and English titles, and severity color indicator.
       - If code is `HSDS`: wrapped with `HazardBorder` and flashes crimson alert.
       - On tap: invokes `hapticMedium()` (or `hapticError()` for HSDS) and selects the syndrome.
    3. Create `mobile/src/components/syndromes/SyndromeGrid.tsx`:
       - 2-column responsive layout (`grid grid-cols-2 gap-3.5`).
       - Iterates through `SYNDROME_TAXONOMY` and renders each `SyndromeCard`.
    4. Create `mobile/src/components/modals/EmergencySOSModal.tsx`:
       - Emergency biohazard lockout modal triggered by center SOS button or HSDS tap.
       - Prominently displays: **"शव विच्छेदन करू नका! / DO NOT CUT CARCASS"** in crimson bold display text.
       - Outlines Anthrax biohazard guidelines, unclotted blood warnings, and automated IDSP alert dispatcher placeholder.
    5. Create `mobile/src/tests/syndromes.test.tsx`:
       - Vitest component tests verifying:
         - Exactly 8 cards rendered in `SyndromeGrid`.
         - HSDS card renders with biohazard indicator and hazard border.
         - Tapping HSDS invokes `hapticError`.
         - All syndrome cards conform to `.field-touch-target` standard.
  </action>
  <verify>
    Run `npm --prefix mobile test -- --run` and confirm all syndrome tests pass.
  </verify>
  <acceptance_criteria>
    - `mobile/src/components/syndromes/SyndromeGrid.tsx` renders all 8 cards
    - `mobile/src/components/modals/EmergencySOSModal.tsx` renders Anthrax biohazard warning
    - `mobile/src/tests/syndromes.test.tsx` passes with 100% green assertions
  </acceptance_criteria>
  <done>
    8-syndrome visual selector with anatomical badges and emergency biohazard modal built and verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assemble Tab Views and Complete Full `App.tsx` Integration</name>
  <files>mobile/src/views/ReportView.tsx, mobile/src/views/DashboardView.tsx, mobile/src/views/AnimalRegistryView.tsx, mobile/src/views/LabReferralView.tsx, mobile/src/App.tsx</files>
  <read_first>mobile/src/store/navigationStore.ts, mobile/src/components/navigation/BottomBar.tsx, mobile/src/components/common/HeaderBar.tsx</read_first>
  <action>
    1. Create `mobile/src/views/ReportView.tsx`:
       - Houses `SyndromeGrid` with quick search / filter bar.
       - Selecting a syndrome opens `FluidDrawer` showing clinical summary, colloquial Marathi terms, and "लक्षण अहवाल नोंदवा / Record Report" primary CTA.
    2. Create `mobile/src/views/DashboardView.tsx`:
       - Embeds `RadarSweep` for local 5km/10km containment perimeter simulation.
       - Displays `CountUpTicker` showing pending offline SQLite reports and active surveillance stats.
       - Includes quick button to inspect `HealthCheckView` diagnostic modal.
    3. Create `mobile/src/views/AnimalRegistryView.tsx`:
       - 12-digit Pashu Aadhaar RFID search bar (`.field-touch-target`).
       - Queries local animal records from SQLite cache.
    4. Create `mobile/src/views/LabReferralView.tsx`:
       - Electronic lab requisition (e-LRF) overview with 48-hour cold-chain shelf-life timers.
    5. Refactor `mobile/src/App.tsx`:
       - Connect `navigationStore` to switch smoothly between `ReportView`, `DashboardView`, `AnimalRegistryView`, and `LabReferralView`.
       - Mount `HeaderBar` at the top, active view in the center, `EmergencySOSModal`, and `BottomBar` at the bottom.
       - Support instant toggling between high-contrast Sunlight light theme and Night barn mode.
  </action>
  <verify>
    Run `npm --prefix mobile run build && npx --prefix mobile cap sync android` and confirm clean compilation and asset embedding into `mobile/android/app/src/main/assets/public/`.
  </verify>
  <acceptance_criteria>
    - `mobile/src/App.tsx` switches between 4 views based on active tab
    - `npm --prefix mobile run build` succeeds in <5s
    - `mobile/android/app/src/main/assets/public/index.html` updated via `cap sync android`
  </acceptance_criteria>
  <done>
    Full mobile application shell with 4 tab views, React Bits animations, and haptics integrated and synchronized with Android container.
  </done>
</task>

</tasks>
