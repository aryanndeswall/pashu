---
phase: 07-gemini-multimodal-triage-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/config.py
  - backend/app/schemas/triage.py
  - backend/app/services/__init__.py
  - backend/app/services/triage_service.py
  - backend/tests/test_triage.py
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
user_setup: []

must_haves:
  truths:
    - "Google GenAI SDK is configured with Gemini 3.7 / 2.5 Flash and strict JSON schema output"
    - "Pydantic triage schema strictly validates the 8 standardized syndromic categories (VSS, NSLS, HSDS, AROS, CMSS, SARF, HES, NAS)"
    - "Rule Zero Anthrax lockout automatically overrides output to CRITICAL_ANTHRAX_LOCK when sudden death or bleeding is detected"
    - "Fallback heuristic engine ensures 100% test passing and sub-millisecond response time when GEMINI_API_KEY is omitted"
  artifacts:
    - "backend/app/schemas/triage.py"
    - "backend/app/services/triage_service.py"
    - "backend/tests/test_triage.py"
  key_links:
    - "triage_service outputs Pydantic TriageResponse validated against 8 syndromic codes"
    - "triage_service evaluates Marathi and Hindi colloquial keywords"
---

<objective>
Implement the Google GenAI SDK integration with Gemini Flash, clinical veterinary prompt instructions, Pydantic v2 strict schemas for the 8 syndromic categories, deterministic Rule Zero Anthrax biohazard lockout, and high-speed fallback heuristic evaluation, verified by comprehensive automated pytest suites.

Purpose: Establishes the cognitive intelligence core of Pashu-Suraksha capable of parsing vernacular voice and lesion markers into actionable clinical data.
Output: Tested triage schemas, Google GenAI client service, neuro-symbolic safety filters, and pytest test suite.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-gemini-multimodal-triage-pipeline/07-CONTEXT.md
@.planning/phases/07-gemini-multimodal-triage-pipeline/07-RESEARCH.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-01 | Zoonotic Misclassification | backend/app/services/triage_service.py | AI fails to flag sudden death with bleeding, missing fatal zoonosis | Neuro-symbolic safety override: Rule Zero hardcoded check overrides AI output to CRITICAL_ANTHRAX_LOCK whenever bleeding/sudden death markers appear. |
| T-07-02 | Service Outage / Rate Limit | backend/app/services/triage_service.py | Network timeout or missing API key crashes triage service | Implement high-speed deterministic heuristic fallback (`EdgeRulesEvaluator`) executing in <5ms. |
| T-07-03 | Schema Hallucination | backend/app/schemas/triage.py | AI outputs arbitrary text or invalid syndrome code | Enforce Pydantic v2 Literal enum validation for the 8 official syndromic codes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Google GenAI Dependency, Config & Triage Schemas</name>
  <files>backend/requirements.txt, backend/app/config.py, backend/app/schemas/triage.py</files>
  <read_first>.planning/phases/07-gemini-multimodal-triage-pipeline/07-RESEARCH.md, backend/app/config.py</read_first>
  <action>
    1. Update `backend/requirements.txt` to add `google-genai>=0.1.1` and `pillow>=10.3.0`.
    2. Update `backend/app/config.py` with:
       - `GEMINI_API_KEY: Optional[str] = None`
       - `GEMINI_MODEL: str = "gemini-2.5-flash"`
       - `AI_INFERENCE_TIMEOUT_SECONDS: float = 3.0`
    3. Create `backend/app/schemas/triage.py`:
       - `SyndromeCode`: Literal["VSS", "NSLS", "HSDS", "AROS", "CMSS", "SARF", "HES", "NAS"]
       - `BiohazardAlert`: Literal["NONE", "WARNING", "CRITICAL_ANTHRAX_LOCK"]
       - `TriageRequest`:
         - `photo_base64: Optional[str] = None`
         - `audio_base64: Optional[str] = None`
         - `audio_transcript: Optional[str] = None`
         - `species: str = "Bovine"`
         - `secondary_symptoms: List[str] = []`
         - `village_lgd_code: Optional[int] = None`
       - `TriageResponse`:
         - `syndrome_code: SyndromeCode`
         - `syndrome_name_en: str`
         - `syndrome_name_marathi: str`
         - `suspected_disease: str`
         - `clinical_confidence: float = Field(..., ge=0.0, le=1.0)`
         - `biohazard_alert: BiohazardAlert`
         - `clinical_rationale: str`
         - `identified_symptoms: List[str]`
         - `immediate_advisory_marathi: str`
         - `immediate_advisory_hindi: str`
         - `recommended_containment_actions: List[str]`
         - `inference_time_ms: int`
         - `model_used: str`
  </action>
  <verify>
    Run `python -c "from app.schemas.triage import TriageRequest, TriageResponse; print('Schemas valid')"`
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/triage.py` strictly validates 8 syndromic codes and biohazard alert literals
    - Pydantic models import cleanly
  </acceptance_criteria>
  <done>
    Google GenAI SDK configured and Pydantic clinical triage schemas created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Triage Service with Gemini Client & Deterministic Fallback</name>
  <files>backend/app/services/__init__.py, backend/app/services/triage_service.py, backend/tests/test_triage.py</files>
  <read_first>backend/app/schemas/triage.py, .planning/phases/07-gemini-multimodal-triage-pipeline/07-RESEARCH.md</read_first>
  <action>
    1. Create `backend/app/services/__init__.py` and `backend/app/services/triage_service.py`:
       - `VETERINARY_SYSTEM_PROMPT`: Veterinary diagnostic instructions for rural India, detailing clinical signs of FMD, LSD, Anthrax, HS, PPR, Mastitis, Blackleg, and Brucellosis.
       - Class `EdgeRulesEvaluator`: Deterministic keyword, symptom, and Rule Zero safety engine:
         - Identifies vernacular Marathi/Hindi terms (*लाळ*, *फोड*, *खुर*, *गाठी*, *घटसर्प*, *मृत्यू*, *रक्त*).
         - Evaluates secondary symptoms.
         - Enforces Rule Zero: sudden death or unclotted orifice bleeding immediately forces `SARF` + `CRITICAL_ANTHRAX_LOCK`.
       - Class `GeminiTriageService`:
         - Initializes `genai.Client` if `settings.GEMINI_API_KEY` is present.
         - `async def triage(self, request: TriageRequest) -> TriageResponse`:
           - If API key is configured and valid, invokes `client.models.generate_content` with system prompt, image parts, and structured JSON output config.
           - Applies Rule Zero deterministic post-filter to prevent AI hallucinations of fatal zoonoses.
           - If API key is None or if GenAI raises an exception / times out, seamlessly falls back to `EdgeRulesEvaluator` without throwing 500 errors.
           - Records sub-second `inference_time_ms`.
    2. Create `backend/tests/test_triage.py`:
       - Test Pydantic schema validation.
       - Test vernacular Marathi audio transcript evaluation (mouth blisters + saliva -> VSS / FMD).
       - Test vernacular LSD evaluation (skin nodules -> NSLS / LSD).
       - Test Rule Zero Anthrax safety override (sudden death + unclotted bleeding -> SARF + CRITICAL_ANTHRAX_LOCK).
       - Test fallback evaluator execution speed (<20ms).
  </action>
  <verify>
    Run `python -m pytest backend/tests/test_triage.py -v` to confirm all unit tests pass 100%.
  </verify>
  <acceptance_criteria>
    - `backend/app/services/triage_service.py` exports `triage_service`
    - Rule Zero Anthrax lockout triggers 100% reliably
    - All tests in `backend/tests/test_triage.py` pass cleanly
  </acceptance_criteria>
  <done>
    Multimodal triage service implemented with Gemini client, Rule Zero safety override, and verified tests.
  </done>
</task>

</tasks>
