---
phase: 07-gemini-multimodal-triage-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/app/api/v1/triage.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_triage_api.py
  - mobile/src/services/aiTriageService.ts
  - mobile/src/components/syndromes/TriageResultCard.tsx
  - mobile/src/tests/TriageResultCard.test.tsx
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
user_setup: []

must_haves:
  truths:
    - "FastAPI backend exposes POST /api/v1/triage/multimodal returning structured clinical triage JSON in <800ms"
    - "aiTriageService calls backend triage endpoint with local fallback for offline barn use"
    - "TriageResultCard renders clinical confidence score, vernacular Marathi/Hindi advisories, and biohazard alert banner"
    - "Anthrax detection triggers red hazard border and high-priority containment directives"
  artifacts:
    - "backend/app/api/v1/triage.py"
    - "backend/tests/test_triage_api.py"
    - "mobile/src/services/aiTriageService.ts"
    - "mobile/src/components/syndromes/TriageResultCard.tsx"
    - "mobile/src/tests/TriageResultCard.test.tsx"
  key_links:
    - "FastAPI triage router mounts under /api/v1/triage"
    - "TriageResultCard displays TriageResponse data with vernacular copy"
---

<objective>
Expose the multimodal triage API endpoint on FastAPI (`POST /api/v1/triage/multimodal`), connect mobile client service `aiTriageService.ts`, build the `TriageResultCard.tsx` visual feedback component with confidence score and vernacular biosecurity directives, and verify end-to-end with automated test suites and production build.

Purpose: Delivers the AI diagnostic output to field veterinarians and farmers with clear vernacular instructions and biohazard safeguards.
Output: Tested triage API endpoint, mobile AI service, visual triage card component, and complete test suites.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-gemini-multimodal-triage-pipeline/07-CONTEXT.md
@.planning/phases/07-gemini-multimodal-triage-pipeline/07-01-PLAN.md
</context>

<threat_model>
| Threat ID | Category | Component | Description | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-04 | Network Latency | aiTriageService.ts | 2G/EDGE network timeout stalls the user during field triage | Set 2.5s client-side timeout; immediately fall back to on-device heuristic scoring if request takes too long. |
| T-07-05 | Farmer Misunderstanding | TriageResultCard.tsx | Smallholder farmer unable to read technical English veterinary terms | Render primary advice in Marathi (प्राथमिक) and Hindi, with bold iconography and audio guidance cues. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build FastAPI Triage Route & Integration Tests</name>
  <files>backend/app/api/v1/triage.py, backend/app/api/v1/__init__.py, backend/tests/test_triage_api.py</files>
  <read_first>backend/app/services/triage_service.py, backend/app/api/v1/__init__.py</read_first>
  <action>
    1. Create `backend/app/api/v1/triage.py`:
       - `router = APIRouter()`
       - `POST /api/v1/triage/multimodal`:
         - Ingests `TriageRequest`.
         - Calls `triage_service.triage(payload)`.
         - Returns `TriageResponse` with HTTP 200.
    2. Mount `triage_router` onto `backend/app/api/v1/__init__.py` under prefix `"/triage"`.
    3. Create `backend/tests/test_triage_api.py`:
       - Test `POST /api/v1/triage/multimodal` with valid Marathi audio transcript and FMD lesion signs.
       - Test `POST /api/v1/triage/multimodal` with Anthrax sudden death signs and verify `CRITICAL_ANTHRAX_LOCK`.
       - Test invalid request payloads return 422.
  </action>
  <verify>
    Run `python -m pytest backend/tests/test_triage_api.py -v` to verify endpoint passes.
  </verify>
  <acceptance_criteria>
    - `POST /api/v1/triage/multimodal` responds with complete TriageResponse
    - All tests in `backend/tests/test_triage_api.py` pass 100%
  </acceptance_criteria>
  <done>
    FastAPI multimodal triage endpoint built, mounted, and verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Mobile aiTriageService, TriageResultCard & Vitest Tests</name>
  <files>mobile/src/services/aiTriageService.ts, mobile/src/components/syndromes/TriageResultCard.tsx, mobile/src/tests/TriageResultCard.test.tsx</files>
  <read_first>mobile/src/services/decisionTreeService.ts, .planning/phases/07-gemini-multimodal-triage-pipeline/07-RESEARCH.md</read_first>
  <action>
    1. Create `mobile/src/services/aiTriageService.ts`:
       - TypeScript interfaces matching `TriageRequest` and `TriageResponse`.
       - `runMultimodalTriage(data: TriageRequest): Promise<TriageResponse>`:
         - Tries calling backend `POST /api/v1/triage/multimodal` with 2.5s timeout.
         - If backend is offline or network fails, executes client-side fallback using `decisionTreeService` keyword logic, returning structured `TriageResponse`.
    2. Create `mobile/src/components/syndromes/TriageResultCard.tsx`:
       - High-contrast visual card matching 60-30-10 palette:
         - Syndrome pill with icon, Marathi title, and English subtitle.
         - Confidence meter (e.g., "९२% विश्वासार्हता / 92% Confidence").
         - Explainable AI Rationale: "लक्षण विश्लेषण (AI Reasoning)".
         - Marathi biosecurity directive card (with audio pronunciation cue icon).
         - Hindi secondary advisory accordion/tab.
         - Immediate containment actions checklist (उदा. विलगीकरण, जंतुनाशक फवारणी).
         - Red pulsating hazard border and emergency warning if `biohazard_alert === 'CRITICAL_ANTHRAX_LOCK'`.
    3. Create `mobile/src/tests/TriageResultCard.test.tsx`:
       - Tests rendering standard FMD triage result with 92% confidence and Marathi advisory.
       - Tests rendering Anthrax biohazard alert with critical lockout warning and red border.
  </action>
  <verify>
    Run `npx vitest run src/tests/TriageResultCard.test.tsx` and `npm run build` in `mobile/`.
  </verify>
  <acceptance_criteria>
    - `TriageResultCard.tsx` renders confidence score, vernacular advice, and containment checklist
    - Tests in `mobile/src/tests/TriageResultCard.test.tsx` pass cleanly
    - Full mobile production build (`npm run build`) exits with code 0
  </acceptance_criteria>
  <done>
    Mobile AI triage client service, visual triage card, and automated tests verified.
  </done>
</task>

</tasks>
